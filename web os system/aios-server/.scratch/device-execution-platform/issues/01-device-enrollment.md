# 01 — Device registry and enrollment

Add Device, DeviceEnrollment and AgentDevice models plus an additive migration. Implement one-time enrollment, hashed device credentials, revoke/rotate and trainer-only management routes. Test expiry, replay, role boundaries and absence of plaintext secrets in DB.
