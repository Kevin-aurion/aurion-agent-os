# 02 — Dedicated device WebSocket and capabilities

Add `/device/ws` with header/subprotocol device authentication, a device connection registry, targeted publish, hello/capability persistence, heartbeat and offline calculation. Query-string device tokens, revoked devices and invalid capability documents must be rejected.
