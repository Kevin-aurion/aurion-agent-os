# 05 — FDE device UI and training selector

Create `/admin/devices` for enrollment and lifecycle management. Add capability-aware online device selection to Computer Use training/workflow configuration. Existing offline bindings remain visible but disabled; MEMBER cannot activate device changes.
