Use the installed `mattpocock-skills:to-tickets` method on `web os system/aios-server/.scratch/aios-client-langflow-platform/spec.md`.

The user already approved proceeding through all six phases without another granularity interview. Treat the following as approved:

- Use local ticket files.
- Use tracer-bullet vertical slices that fit one fresh Grok CLI context.
- Preserve a strict dependency frontier and separate high-risk Model Gateway, Production isolation, and final negative-suite tickets.
- Workbench V2 must be split by complete user journeys, not by generic component layers.
- Phase 6 should implement honest hardening foundations only; do not claim full multi-tenant production readiness.
- Every ticket needs a small To-Do checklist, concrete acceptance criteria, verification commands, positive/negative tests, exact likely files, existing patterns to reuse, and explicit must-not-modify boundaries. This intentionally extends the default ticket template because the Claude Code/Grok/Fable workflow consumes these files mechanically.

Return:

1. A numbered dependency-ordered ticket index.
2. For every ticket, the complete Markdown body ready to save as one file under `web os system/aios-server/.scratch/aios-client-langflow-platform/issues/`.

Do not edit files in this session.
