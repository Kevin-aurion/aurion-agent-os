# 03 — Durable device tasks and artifacts

Implement DeviceTask and DeviceArtifact persistence, idempotent creation, ACK/lease/renew/progress/result/cancel/reclaim and safe artifact upload with digest, redaction and TTL. Cross-device access and terminal-result overwrite must fail closed.
