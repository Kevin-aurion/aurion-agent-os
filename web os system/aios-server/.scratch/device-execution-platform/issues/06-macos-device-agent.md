# 06 — macOS Device Agent and real execution bridge

Add Keychain device identity, enrollment, dedicated WSS reconnect, capability probes, local stdio MCP runtime, local approval, screenshots and real result reporting. Build with xcodebuild. Document the Windows contract honestly; do not claim a Windows runtime exists.
