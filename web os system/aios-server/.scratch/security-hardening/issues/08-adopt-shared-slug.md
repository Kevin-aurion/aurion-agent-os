# 08 — 全站改用共用 CJK slug（收斂重複實作）

**What to build:** 不論從哪個入口（員工、技能、口述訓練、AI 組裝）用中文命名，都得到一致、非空、可辨識的 slug；重複的 slug 實作收斂成一份。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 各入口改用共用 slug 工具（移除重複實作）
- [ ] 中文命名在各入口都得到相同且非空的 slug
- [ ] 既有 slug 唯一化行為（衝突加後綴）保留
- [ ] 帶有未提交 WIP 的檔案採最小改動；不得混入 WIP 的無關變更
