Work only in `web os system/aios-studio/`. Fix one browser-discovered UI defect: after Compile -> Native succeeds, the action note currently prefers `compiled.digest`. The server-wide redactor can conservatively mask the 12-character digest prefix, producing a confusing `[REDACTED...]` success message. Change the success note to show a short immutable compiled artifact id instead (with an ellipsis), never the digest. Add or adjust a small pure presentation/unit test if appropriate. Run `npm test`, `npm run typecheck`, and `npm run build`. Do not commit or push. Report exact result.
